source /opt/workshops/elastic-retry.sh
export $(curl http://kubernetes-vm:9000/env | xargs)

cd /workspace/workshop
cp -rf src/recorder-java/_courses/o11y--course--field--100-otel-k8s--main/_challenges/04-annotations/solve .

./build.sh -d force -s recorder-java
